#include <iostream>

#ifndef START_HPP
#define START_HPP

// This header file contains declarations for functions "start", "lat", "lon" and "loc"
std::string start();
std::string lat(std::string s);
std::string lon(std::string s);
std::string loc(std::string s);

#endif